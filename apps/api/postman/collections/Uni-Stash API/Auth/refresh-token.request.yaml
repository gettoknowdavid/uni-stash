$kind: http-request
name: Refresh Token
method: POST
url: '{{baseUrl}}/api/v1/auth/refresh'
order: 4000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refresh_token": "{{refreshToken}}"
    }
