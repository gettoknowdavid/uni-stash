$kind: http-request
name: Verify Email
method: POST
url: '{{baseUrl}}/api/v1/auth/verify-email'
order: 2000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "token": "{{verificationToken}}"
    }
